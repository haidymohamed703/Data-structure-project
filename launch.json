{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run Restaurant System",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/run.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "build restaurant",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb"
        }
    ]
}